{
  "name": "voice-chain-flue-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Flue agent (the harness/brain) that sits between STT and TTS in the pipecat voice pipeline",
  "scripts": {
    "dev": "flue dev",
    "start": "flue dev",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "dependencies": {
    "@flue/runtime": "1.0.0-beta.9",
    "@hono/node-server": "^2.0.3",
    "hono": "^4.12.0"
  },
  "devDependencies": {
    "@flue/cli": "1.0.0-beta.9",
    "@types/node": "^26.1.1"
  }
}
